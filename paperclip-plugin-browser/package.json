{
  "name": "paperclip-plugin-browser",
  "version": "0.1.0",
  "description": "Guarded Outlook-on-the-web automation (Stagehand/Playwright) for when the Graph API can't be used. Read and draft only; never sends. A mesh-runner fallback capability.",
  "type": "module",
  "scripts": {
    "login": "tsx src/cli/login.ts",
    "inbox": "tsx src/cli/inbox.ts",
    "draft": "tsx src/cli/draft.ts",
    "typecheck": "tsc --noEmit"
  },
  "engines": { "node": ">=18" },
  "license": "MIT",
  "dependencies": {
    "@browserbasehq/stagehand": "^3.0.0",
    "dotenv": "^16.4.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.4.0"
  }
}
